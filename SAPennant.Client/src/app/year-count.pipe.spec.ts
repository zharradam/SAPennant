import { YearCountPipe } from './year-count.pipe';

describe('YearCountPipe', () => {
  it('create an instance', () => {
    const pipe = new YearCountPipe();
    expect(pipe).toBeTruthy();
  });
});
